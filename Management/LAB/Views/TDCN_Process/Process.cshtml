@{
    ViewData["Title"] = "Thăm Dò Chức Năng - Đang thực hiện";
    Layout = "_LayoutTDCN";
    var lstDevice = ViewData["lstDevice"] as List<Device>;
}

<div class="tdcn-container">
    <div class="tdcn-left">
        <div class="tdcn-left-search">
            @await Html.PartialAsync("_TDCN_Process_Search")
        </div>
        <div id="process_listPatient" class="tdcn-left-list">
            @await Html.PartialAsync("_TDCN_Process_ListPatient")
        </div>
    </div>
    <div class="tdcn-right">
        <div id="process_patientInfo" class="tdcn-right-info">
            @await Html.PartialAsync("_TDCN_Process_PatientInfo")
        </div>
        <div id="process_patientService" class="tdcn-right-service">
            <div id="process_tdcn-right-service-group" class="tdcn-right-service-group">
                @await Html.PartialAsync("_TDCN_Process_GroupButton")
            </div>
            <div id="process_tdcn-right-service-gridview" class="tdcn-right-service-gridview">
                @await Html.PartialAsync("_TDCN_Process_ListService_UploadPdf")
            </div>
        </div>
    </div>
</div>

@await Html.PartialAsync("_Loading")

<!-- From chọn Device -->
<div class="modal" id="addDeviceForm" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" role="dialog" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="tdcn-right-select-device-title">
                <div class="title">CHỌN THIẾT BỊ</div>
            </div>
            <div class="tdcn-right-select-device">
                <div class="title">Thiết bị</div>
                <div class="content">
                    <select id="tdcn_getsample_select_device_select" name="tdcn_getsample_select_device_select" class="form-select height-28">
                        <option value=""></option>
                        @if (lstDevice != null)
                        {
                            foreach (var item in lstDevice)
                            {
                                <option value="@item.Id">@item.Name</option>
                            }
                        }
                    </select>
                </div>
            </div>
            <div class="tdcn-right-select-device-button">
                <button id="tdcn_getsample_select_device_button" class="btn btn-success btn-blue" data-bs-dismiss="modal" onclick="Process_SelectDevice()">
                    <i class="bi bi-download"></i> Lưu
                </button>
            </div>
        </div>
    </div>
</div>
<!-- From chọn Device -->